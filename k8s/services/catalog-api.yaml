apiVersion: v1
kind: ConfigMap
metadata:
  name: catalog-api-config
  namespace: eshop
data:
  OTEL_EXPORTER_OTLP_ENDPOINT: "http://otel-collector.eshop.svc.cluster.local:4318"
  OTEL_EXPORTER_OTLP_PROTOCOL: "http/protobuf"
  OTEL_TRACES_SAMPLER: "always_on"
  OTEL_SERVICE_NAME: "catalog-api"
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: catalog-api
  namespace: eshop
  labels:
    app: catalog-api
spec:
  replicas: 1
  selector:
    matchLabels:
      app: catalog-api
  template:
    metadata:
      labels:
        app: catalog-api
    spec:
      imagePullSecrets:
        - name: nexus-registry
      containers:
        - name: catalog-api
          image: 192.168.1.175:8082/eshop/catalog-api:v2
          ports:
            - containerPort: 8080
          envFrom:
            - configMapRef:
                name: catalog-api-config
          env:
            - name: ConnectionStrings__catalogdb
              valueFrom:
                secretKeyRef:
                  name: eshop-secrets
                  key: catalogdb-connection
            - name: ConnectionStrings__EventBus
              valueFrom:
                secretKeyRef:
                  name: eshop-secrets
                  key: eventbus-connection
          readinessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /alive
              port: 8080
            initialDelaySeconds: 15
            periodSeconds: 30
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
---
apiVersion: v1
kind: Service
metadata:
  name: catalog-api
  namespace: eshop
spec:
  selector:
    app: catalog-api
  ports:
    - port: 8080
      targetPort: 8080